/**
 * Compute domain records: identifiers, lane addressing, definitions for the three
 * compute types (services, jobs, tasks), and the typed state returned by each
 * workflow's {@code getState()} query.
 */
package com.compute.model;
